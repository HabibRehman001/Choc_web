# Leave unset for local dev — Vite proxies /api to the backend on port 3000.
#
# REQUIRED on Vercel (Settings → Environment Variables → Production):
#   VITE_API_BASE_URL = https://choc-web-api.onrender.com
# Use your actual Render backend URL. No trailing slash. Redeploy after changing.
